# REPLACED — see the deploy-* and validate-and-plan workflows.
name: deprecated
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "deprecated"
